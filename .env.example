# =================================================================
# 📋 RADAR CONTA CERTA — Modelo de Variáveis de Ambiente
# =================================================================
# Copie este arquivo para `.env` e preencha com seus valores reais:
#   cp .env.example .env
#
# ⚠️  NUNCA commite o `.env` real — apenas este modelo vazio.
# =================================================================

# =================================================================
# 🗄️ BANCO DE DADOS (PostgreSQL)
# =================================================================
DATABASE_URL="postgresql://radar:radar_secret@localhost:5432/radar_conta_certa?schema=public"

# =================================================================
# 🔐 AUTENTICAÇÃO (JWT)
# =================================================================
# Gere uma chave forte: openssl rand -base64 48
JWT_SECRET="troque_por_uma_chave_forte_de_48_chars_ou_mais"
JWT_EXPIRATION="7d"

# =================================================================
# 🌐 BACKEND (NestJS)
# =================================================================
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# =================================================================
# 🎨 FRONTEND (Next.js)
# =================================================================
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_APP_NAME="Radar Conta Certa"

# =================================================================
# 📧 E-MAIL (opcional — Sprint futura)
# =================================================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=

# =================================================================
# ☁️ STORAGE (opcional — S3/MinIO para uploads)
# =================================================================
# S3_BUCKET=
# S3_REGION=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=