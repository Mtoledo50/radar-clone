# Não manda lixo pra imagem (build mais rápido e seguro)
node_modules
dist
.env
.env.*
*.log
.git

# Arquivos compactados
*.zip
*.tar.gz
*.rar
*.7z

# Backups
backup/
*.backup

# Node modules (se não estiver lá)
node_modules/